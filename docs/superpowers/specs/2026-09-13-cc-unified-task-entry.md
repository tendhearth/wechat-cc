# CC 统一任务入口：持续交付与验收

用户目标：日常执行任务只需打开 CC，由 CC 管理 Claude、Codex 和其他合适的 agent / LLM；微信也是同一后台的入口。保持陪伴与工作的边界，减少转述、重复配置和丢失上下文。用户已授权持续推进，不逐步请求确认。

本文件延续 `2026-09-12-cc-task-entry-scope.md` 的两栏工作台和已实现的任务模型，扩大后续能力验收，不把已有三段任务循环等同于整个目标完成。

## 当前证据与下一步

2026-09-13 首轮审计基线 `6bb59ab8`。审计确认了影响优先级的问题：工作台的原生配置隔离同时移除了专业任务所依赖的工具与部分项目说明。后续已补原生项目能力、微信任务控制和图片/文件材料链路，以下按交付证据更新，仍不能宣称 CLI / App 大部分能力已覆盖。

| 用户需要 | 当前证据 | 尚需证明 |
| --- | --- | --- |
| 日常任务全程在 CC 完成 | 单任务、并发、原生恢复、交接及实时对话已有独立验证记录；Claude 项目规则/技能/直接配置 MCP 和 Codex 直接配置 MCP/搜索路径；任务图片/文件、任务级模型与思考强度选择、恢复及排队设置留存已有原生传输及真实浏览器链路证据 | 更多原生能力；后台生命周期在当前 macOS 原生版本的已验证范围之外仍需扩展；附件 Windows 支持、长期存储管理 |
| Claude / Codex 不再需要分别打开 | 原生 SDK / app-server，已测试闭环；支持工具的按次批准和配置策略保留，未带入能力留任务记录 | 原生插件、通用 MCP 表单与 OAuth 界面、全局指令的选择性继承；跨平台后台执行资源回收 |
| 多项目不串 | 会话、成果、草稿隔离，同目录排队 | 独立工作副本及更完整代码检查；并发下的新功能仍须复测 |
| 跨执行者合作 | 固定版本检查、选择意见交回原会话；显式选择原始附件，交接和交回使用已记录字节，恢复预览保留材料顺序 | 工具上下文、后台状态和恢复的完整性；有依据的管理与有限自动协作 |
| 微信控制同一件事 | 已知项目创建、查询/停止/补充、权限/答案、订阅完成提醒、固定版本正文分页及显式索取成果文件；跨通道请求和严格发送回执已有隔离 HTTP 验证 | 真实微信账号验收、任务语音控制；当前入口仍为显式文本命令 |
| 其他 agent / LLM | 独立 API 任务适配器支持文字/图片材料、受限文件工具、新建文本成果及完整工具记录续接；已通过本地兼容接口验证 | 没有 shell、MCP、后台工作或供应商原生历史；真实 API 模型质量与平台兼容尚需验证；Cursor/agy 仍只接普通聊天 |
| 多设备入口 | 微信可连接同一后台 | 尚无跨主机执行迁移证据；不能把本机恢复写成接管外部活跃进程 |
| 陪伴 | 此刻 / 回忆 / 一起做分工已定；新微信工作消息有独立来源标记，不进入个人记忆提取；原生任务不注入 CC 私人权限 | 历史无来源标记的数据不能凭内容猜分类；后续新通道和工具继续验证边界 |

## 实现原则

- 参考 Paseo 的后台持有会话、多客户端接入，Orca 的项目隔离与成果检查，Codex 的原生会话 / turn / item 协议。引用或借用前核对主仓库的实现；五个项目的确切身份与固定来源已整理在[参考说明](../../research/2026-09-14-cc-agent-workbench-references.md)。
- 用户只看到任务、执行者、结果和需要处理的事项；扩展功能仍按需展开，不新增常驻流程图、终端墙或设置表单。
- 各通道调用同一任务服务。微信的普通聊天不自动变成当前任务输入，也不使用全局“最后一个任务”来猜路由。
- 显示“已传达”须有执行器确认。只有确实会自动进入下一轮的消息才这样显示；保留会话的 queue 只显示已保存。失败或停止后的未确认回执保留，不自动重发。
- 保留任务、执行轮次、请求和成果版本身份。过期 / 错任务 / 错所有者 / 重复授权不得作用于另一请求。
- 恢复原生项目能力不等于恢复 CC 伴随服务的全局权限。不得继承 companion prompt、微信发送 / 私人记忆凭证或 daemon bypass。
- 不能用测试桩证明真实 harness 支持。协议 / 服务测试后，使用隔离目录、自己创建的规则和工具做真实验收，不操作用户未知的 MCP 服务或发送真实微信消息。

## 本轮交付检查

1. 核对原生配置继承的精确边界，按受支持接口修复可落地的能力，并把尚未支持的部分如实记录。
2. 微信任务控制沿用同一任务服务，补齐运行中输入和待处理事项；覆盖通道所有权、跨任务与重复处理。
3. 定向测试、完整类型检查、原生隔离验证与独立代码审查。每个可独立验证的变化单独提交。此前不自动推送；2026-09-14 收尾时 owner 已明确授权推送 dev。仍不替换用户正在运行的正式 bot。

本轮完成不表示总目标完成。上表中未验证项继续保留，后续以用户仍需离开 CC 的具体原因决定优先顺序。

本轮的原生版本、测试范围、审查修正、参考项目及明确限制见[交付记录](../reports/2026-09-13-cc-native-capabilities-and-wechat.md)。尚未运行用户真实 MCP，也没有发送真实微信消息或替换正式 bot。

附件这一轮的协议差异、实际链路、审查修正与平台限制见[附件交付记录](../reports/2026-09-13-cc-task-attachments.md)。任务级模型与思考强度已补；配置与实际回报分开、恢复确认绑定选择、原生 preset 与执行 model 的区别和完整验证结果见[模型交付记录](../reports/2026-09-13-cc-task-execution-models.md)。该模型批次当时的全仓测试有 16 个设置页 HTTP 超时，属于那次运行的历史结果；最新全仓结论以[收尾记录](../reports/2026-09-14-cc-workbench-wrapup.md)为准。后台子助手、主回复后自动回复、输入确认、原生命令回收已在本轮已验证范围内补齐，见[后台记录](../reports/2026-09-13-cc-background-runtime.md)。进程脱离后的追踪、通用自动完成证明和 Windows 支持仍不包含在该结论里。后续继续以[真实任务覆盖](2026-09-13-cc-professional-coverage.md)决定优先顺序。

2026-09-14 收尾：微信 Task 5 的不可变成果发送已交付，独立 API 任务执行路径已补齐本批限定范围。当前导览见[工作台说明](../../cc-workbench.md)，验证与限制见[收尾记录](../reports/2026-09-14-cc-workbench-wrapup.md)。本次收尾不再扩展功能。
