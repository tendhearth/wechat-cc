# 工作台运行中沟通验证

日期：2026-09-13。工作区：`wechat-cc-cc-kit`，分支：`codex/cc-workbench-v1`，本轮基线：`c74aae4a`。

## 本轮交付

- Codex 可向当前一轮补充要求，以原生确认作为已交付依据；不会暗中新开会话。
- Claude 明确使用“加入下一轮”。补充持久化到任务队列，本轮正常完成、执行进程退出、目录身份及原会话确认后才接续。停止、失败、重启都保留未发送内容；可撤回待发项。
- 原生结构化问题显示为独立表单，支持选项、自由回答、多选及跳过；回答绑定原任务与请求，和执行权限分开。等待用户时暂停执行闲置计时，回答后获得完整的执行等待预算。
- 全应用保留简短的待处理入口，可准确返回任务。新增问题或权限且用户离开当前任务时才通知；重开页面不补发旧通知，未新增请求不重复通知。
- 问题草稿与补充重试编号在当前窗口保存。轮询、刷新、切页及旧请求迟到不清除新文字，也不把已交付内容再次发送。首尾空白采用一致的发送与回执校验方式。
- “未发送”与“未确认交付”分开显示。原生交付结果不确定时先让用户查看当前对话，不自动重试，不展示底层错误全文。

## 自动验证

| 检查 | 最终结果 |
| --- | --- |
| `bun --bun vitest run --maxWorkers=4 --exclude src/daemon/settings-panel.test.ts` | 508 文件、6727 项通过；已有 1 文件、10 项跳过 |
| `bun run typecheck` | 通过 |
| 原生 Rust `workbench_proxy_tests` | 通过；四个新增接口的正确方法与路径、错误方法与扩展路径均有断言 |
| 数据库迁移专项 | 3 文件、47 项通过；新增 v50 指纹，旧版本锁及精确表集合断言保留 |
| 最终 UI 专项 | 102 项通过，包括有／无 sessionStorage 的离页重建竞态 |
| 桌面身份与接口集成 | 2 文件、42 项通过；新增四项读取真实 operator 凭据文件的 HTTP 测试 |
| `git diff --check` | 通过 |

全仓命令明确排除了既存的 `src/daemon/settings-panel.test.ts`，没有改配置或删除断言。该文件的 HTTP token 往返用例另行单独运行，仍在 5000ms 超时；本轮对该测试及 `settings-panel.ts` 的差异为空。因此不声称未排除任何用例的全仓全绿。

补齐接口集成后的一次默认并发回归中，另有两项未修改的 agy 子进程测试碰到 5000ms 超时；其单独四项复查通过。最终只把测试并发降到 4，没有增加超时或新增跳过，完整运行得到上表结果。

新增回归先观察失败再修复，覆盖任务／运行／请求归属、重复提交、过期回答、原生拒绝、停止与恢复、排队期间目录被替换、发送前失败、磁盘写入失败时仍可停止，以及回答后的闲置预算。另由独立代理审查原生协议、服务生命周期和 UI 连接，所发现的交付与草稿问题已补回归。

## 原生实测与能力边界

使用临时空项目、合成文字及新会话，未导入个人历史，未改用户配置，拒绝执行权限。每项只运行一个短测试：

| 原生检查 | 结果 |
| --- | --- |
| Codex 当前轮补充 | 通过：`turn/steer` 返回确认，最终回复包含补充中指定的唯一标记 |
| Claude AskUserQuestion | 通过：收到原问题，提交多选答案，执行者继续并回传答案与唯一标记 |
| Codex 同步结构化问题 | 未触发，不能算实测通过；模型正常结束，没有 question RPC |

本机 Codex 0.153.4 的 `default_mode_request_user_input` 为关闭状态。新会话沿用 Default 模式，其同步工具默认只允许 Plan；本轮未为触发问题而切换模式或打开实验开关。同步 RPC 接收、回答及过期拒绝已有协议测试，但不能把它等同于当前默认模式的产品能力。依据：[同版本模式条件](https://raw.githubusercontent.com/openai/codex/rust-v0.153.4/codex-rs/tools/src/tool_config.rs)、[原生同步处理器](https://raw.githubusercontent.com/openai/codex/rust-v0.153.4/codex-rs/core/src/tools/handlers/request_user_input.rs)。

Codex 另有 `request_user_input_async`，经 `agentMessage` 携带问题，答案通过新用户消息返回。它有不同生命周期，本轮尚未把这组字段适配成问题卡片；已有文本消息展示不等于异步结构化问答支持。依据：[同版本异步处理器](https://raw.githubusercontent.com/openai/codex/rust-v0.153.4/codex-rs/core/src/tools/handlers/request_user_input_async.rs)。

## 浏览器实际验证

隔离页面加载生产 UI 和 CSS，仅将接口及系统通知替换为内存夹具，未向真实用户任务写入测试消息：

1. 问题选择、自由文字、Tab 导航与提交成功；权限卡与问题卡分开。
2. 填写后刷新，原问题的草稿恢复；换任务不串回答。
3. 提交失败时保留文字；重试只有一条新增排队记录；撤回后显示“已撤回”。
4. 含首尾空白的补充正常入队并清空已提交草稿。
5. 离开工作台后，待处理入口保持；新增问题只记录一次通知，点击入口返回对应任务，轮询不重复通知。
6. 未发送记录可放回输入框；两个执行者显示不同的补充按钮和真实交付状态。

系统通知的分发逻辑与失败降级已验证，**没有进行真实 macOS 通知送达验收，也没有重编完整 Tauri app**。

主开发预览 `http://127.0.0.1:4187/` 已重启到最新后台。重启前确认没有活动任务；重启后原有 7 项任务及其终态保持，旧对话与成果入口可读取，没有注入演示任务。未改冻结角色资产或运行占位资产构建脚本，未推送分支。

## 真实入口复核发现的集成遗漏

功能提交 `131e83a1` 后，直接访问真实开发入口发现四个新接口被 operator 的独立路由范围拒绝。代理白名单和 admin 层级已添加，但桌面凭据的路由集合漏加，之前仅使用 admin session 凭据的测试没有覆盖这一层。

后续补齐四个确切方法／路径，并保留所有原断言。新增的四项 operator 文件凭据 HTTP 测试先全部复现 403，再通过；普通 trusted 身份、错误方法和扩展路径仍被拒绝。最后从 4187 的真实代理验证：attention 返回 200 和空任务集，三个写接口对不存在的合成任务／请求返回各自的 409 过期错误，证明已到达正确的业务校验，没有创建或修改任务。这项修复单独提交，未修改用户全局配置或放宽执行者权限。
