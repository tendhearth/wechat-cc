# 对话图片缩略图

按已确认的交互落实：发送前有可移除预览；历史消息内直接显示图片；点击放大、保留下载；普通文件继续使用文件卡片。PNG/JPEG/WebP/GIF 可内联，SVG 等不作为自动加载的图片。

图片读取沿用桌面 operator 的任务附件接口，按任务和附件 ID 缓存。缩略图按可见区域加载，远程缓存最多 32 项；草稿移除/发送成功、页面销毁会释放对应 Blob URL。不把图片字节或凭据写入 sessionStorage。

生成的图片成果按保存时间关联到之前的回复，点击直接弹窗，不要求先展开成果面板。现有成果元数据没有显式 turn ID，因此这里使用时间顺序关联，不宣称是新的回合归属协议。随后出现的新回复不会把旧图挪走。

验证：

- 附件与缩略图单测：图片/普通文件分流、安全 MIME 限制、任务隔离、并发读取去重、失败重试、销毁后拒绝迟到结果、URL 释放、历史成果位置。
- 全仓 Bun 8370 项通过（10 项跳过）；类型检查通过；模块边界 0 error、7 条既有 warning。
- Chromium → 生产页面模块 → host proxy → HTTP → SQLite 验证：文件选择后预览、发送后缩略图、弹窗、重开对话、注入 503 后重试、生成图片内联及弹窗。执行者为合成 fixture。
- 浏览器证据：`/private/var/folders/yc/y9bc_lbd69z5_3_dqbt5bn6c0000gn/T/cc-background-browser-evidence-w9duU9/`。
- 独立复核指出的成果弹窗、增量消息加载、提交后释放草稿 URL 三项已修复。
- Node 全仓出现一项既有 chunked-upload 测试的 EPIPE；该文件独立重跑 66 项通过，未更改该测试或后台代码。

本轮不替换已安装桌面包，也不混入工作树中其他页面/品牌/发版改动。
