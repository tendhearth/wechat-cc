# 画像依据 / 来源改正验证截图

这些图来自 `bun scripts/memory-evidence-browser-smoke.ts`，使用生产页面、样式及记忆模块，连接实际内部 HTTP 路由和临时 SQLite/文件。内容、模型输出与启动挂载是测试夹具，不含主人的记忆，也不代表原生或真实模型验收。

- `profile-1280.png`：卡片上的依据入口。
- `evidence-1280.png`：查看当前来源并改正。
- `latest-source-390.png`：来源发生变化时，原文与保留草稿分开；核对后明确更新编辑基线。

不在正式前端目录内，不随 frontendDist 打包。
