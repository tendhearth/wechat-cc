# CC 工作台浏览器预览

`preview.html` 使用生产 `workbench.js` 和 `styles/workbench.css`，数据为页面右上角明确标注的内存样例。顶部可切换空态、执行中、已完成；样例支持创建、续接、停止、成果预览和指定版本确认。`preview-1280.png` 和 `preview-760.png` 是完成态在两个验收宽度下的截图。它们只用于布局和前端交互检查，不代表真实模型、微信或原生文件夹选择器验证。
