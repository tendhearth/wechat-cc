# Revised material / C — native evidence

The QA app was rebuilt from the revised asset manifest. These are unretouched
CUA captures of its four actual Tauri windows on white and charcoal **in-window
QA backdrops**. `native-review.png` arranges the captures without scaling.
The UI reached loop 4 (at least three complete cycles); all four windows loaded
16 unique playback frames with zero image errors. Actual sprite rectangles were
48×48 / 96×96 / 128×128 / 256×256. See `native-loop-state.txt` and `results.json`.

Dark has a clearer C/shoulder contour while retaining a charcoal center. 48px on
charcoal remains subdued; no claim of final aesthetic approval. Pointer highlights
away from the sprites belong to the capture tool. External physical-desktop
compositing remains unverified, as explained in the parent QA README.
